server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

#启用hystrix
feign:
  hystrix:
    enabled: true         #在Feign中开启Hystrix